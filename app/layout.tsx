import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  metadataBase: new URL("https://salaryscope.in"),
  title: {
    default: "In-Hand Salary Calculator India FY 2025-26 | SalaryScope",
    template: "%s | SalaryScope",
  },
  description:
    "Calculate your exact take-home salary after tax, PF, and deductions. Free CTC to in-hand calculator and tax tools for Indian professionals.",
  robots: { index: true, follow: true },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="flex flex-col min-h-screen">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
