"use client";
import { useState } from "react";
import { estimatePercentile, formatINR } from "@/lib/calculations";
export default function PercentileEstimator(){const [role,setRole]=useState('Software Engineer');const [city,setCity]=useState('Bengaluru');const [ctc,setCtc]=useState(1200000);const result=estimatePercentile(role,city,ctc);return <div className="rounded-2xl border border-border bg-white p-6 shadow-sm md:p-8"><div className="grid gap-5 md:grid-cols-3"><label className="text-sm font-semibold">Role<select className="mt-2 w-full rounded-lg border border-border bg-white px-3 py-3" value={role} onChange={e=>setRole(e.target.value)}><option>Software Engineer</option><option>Data Analyst</option><option>Product Manager</option></select></label><label className="text-sm font-semibold">City<select className="mt-2 w-full rounded-lg border border-border bg-white px-3 py-3" value={city} onChange={e=>setCity(e.target.value)}><option>Bengaluru</option><option>Hyderabad</option><option>Pune</option><option>Delhi NCR</option><option>Chennai</option></select></label><label className="text-sm font-semibold">Annual CTC (₹)<input className="mt-2 w-full rounded-lg border border-border px-4 py-3" type="number" value={ctc} onChange={e=>setCtc(Number(e.target.value))}/></label></div><div className="mt-8 rounded-xl bg-light-bg p-6"><p className="text-sm text-muted">Approximate market position</p><p className="mt-2 text-4xl font-bold text-primary">Top {100-result.percentile}%</p><p className="mt-3 text-sm leading-6 text-muted">For a {role.toLowerCase()} in {city}, ₹{formatINR(ctc)} is around the {result.percentile}th percentile in our indicative market bands.</p></div><p className="mt-5 text-xs leading-5 text-muted">Indicative estimate based on public salary ranges, not an official survey. Company, experience, skills and equity can materially change the comparison.</p></div>}
