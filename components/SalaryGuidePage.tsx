import type { Metadata } from "next";
import Link from "next/link";
export function guideMetadata(title:string,description:string):Metadata{return {title,description};}
export default function SalaryGuidePage({title,intro}: {title:string;intro:string}){const faqs=[['Are these salary figures official?','No. They are indicative public-data estimates intended for planning and comparison.'],['How should I compare a salary offer?','Compare fixed pay, variable pay, employer contributions, location, role scope and expected growth—not only CTC.']];const jsonLd={"@context":"https://schema.org","@type":"Article",headline:title,description:intro,author:{"@type":"Organization",name:'SalaryScope'}};return <div className="container-site py-14 md:py-20"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><article className="mx-auto max-w-3xl"><p className="font-mono text-xs font-semibold uppercase tracking-[.18em] text-primary">Salary guide / 2026</p><h1 className="mt-3 text-4xl font-bold tracking-tight md:text-5xl">{title}</h1><p className="mt-6 text-lg leading-8 text-muted">{intro}</p><div className="mt-10 grid gap-4 sm:grid-cols-3">{[['P25','₹6–10L'],['P50','₹10–18L'],['P75','₹18–30L']].map(([a,b])=><div key={a} className="rounded-xl border border-border bg-white p-5"><p className="text-sm text-muted">{a} indicative band</p><p className="mt-2 text-xl font-bold text-primary">{b}</p></div>)}</div><h2 className="mt-12 text-2xl font-bold">How to use this estimate</h2><p className="mt-4 leading-8 text-muted">Salary ranges vary by experience, interview performance, company tier, skills, location and the mix of fixed and variable compensation. Treat these bands as directional context. For a personal number, run the <Link className="font-semibold text-primary" href="/in-hand-salary-calculator">in-hand salary calculator</Link> and compare the actual offer structure.</p><h2 className="mt-12 text-2xl font-bold">Frequently asked questions</h2><div className="mt-4 divide-y divide-border">{faqs.map(([q,a])=><details key={q} className="py-4"><summary className="cursor-pointer font-semibold">{q}</summary><p className="mt-2 text-muted">{a}</p></details>)}</div><p className="mt-10 border-l-4 border-accent pl-4 text-sm leading-6 text-muted">Disclaimer: SalaryScope does not represent any employer or government body. Verify compensation with the employer and current tax rules.</p></article></div>}
